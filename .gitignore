# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Dependency / build output (added by tooling, not source)
/vendor/
/node_modules/
*.min.css.map
*.min.js.map

# PHP / Moodle tooling
.phpunit.result.cache
/.phpcs-cache
/moodle-plugin-ci.log

# Local environment
.env
.env.*
*.local

# Secrets / credentials — never publish
*.pem
*.key
id_rsa*
id_ed25519*
*.token
auth.json

# AI / coding-agent tooling — internal, never publish
.claude/
.cursor/
.cursorrules
.aider*
.continue/
.windsurf/
.github/copilot-instructions.md

# Confidential design handoff / internal docs — keep out of the public repo
design_handoff_azmsi_platform/
**/design_handoff_azmsi_platform/
